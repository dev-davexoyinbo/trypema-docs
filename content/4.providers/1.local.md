---
title: Local Provider
description: In-process rate limiting.
navigation:
  icon: i-lucide-cpu
---

The local provider stores per-key state in memory and is intended for single-process limiting.

Best practices:

- Use `run_cleanup_loop()` if you have many keys.
- Choose `rate_group_size_ms` based on the accuracy/performance trade-off you want.
