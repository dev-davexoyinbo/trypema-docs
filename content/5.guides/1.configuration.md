---
title: Configuration & Tuning
description: Recommended defaults and trade-offs.
navigation:
  icon: i-lucide-sliders
---

Trypema exposes three main tuning knobs (per provider):

- `window_size_seconds`
- `rate_group_size_ms`
- `hard_limit_factor` (suppressed strategy)

## Recommended starting point

```rust
use trypema::{HardLimitFactor, RateGroupSizeMs, WindowSizeSeconds};
use trypema::local::LocalRateLimiterOptions;

let opts = LocalRateLimiterOptions {
    window_size_seconds: WindowSizeSeconds::try_from(60).unwrap(),
    rate_group_size_ms: RateGroupSizeMs::try_from(10).unwrap(),
    hard_limit_factor: HardLimitFactor::try_from(1.5).unwrap(),
};
let _ = opts;
```

## Trade-offs

- Larger windows smooth bursts but recover slower.
- Larger coalescing is faster/smaller but makes backoff hints less precise.
